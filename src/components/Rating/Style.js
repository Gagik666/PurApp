import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    wrapper: {
        justifyContent: "center",
        alignItems: "center",
    },
    txt: {
        position: "absolute",
        fontSize: 22,
        fontWeight: "bold",
        color: "#26294C",
        paddingTop: 10,
    }
})