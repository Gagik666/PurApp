import { Dimensions, StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    wrapper: {
        width: "80%",
        paddingStart: 24,
        paddingTop: 8
    },
    txtError: {
        color: "#FF0000",
        fontSize: 18
    }
})