import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {paddingTop: 16},
  txt: {
    color: "#17223B",
    fontWeight: "600",
    fontSize: 18,
    lineHeight: 22,
  },
});
